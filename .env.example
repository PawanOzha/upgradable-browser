# AI Provider Configuration
# Copy this file to .env and configure your preferred AI provider

# ============================================
# Default AI Provider
# ============================================
# Options: ollama, anthropic, openai, gemini
# Recommended: anthropic (most reliable for JSON formatting)
VITE_DEFAULT_AI_PROVIDER=openai

# ============================================
# Anthropic Claude (Recommended)
# ============================================
# Get your API key from: https://console.anthropic.com/
# Best for: Reliable JSON formatting, high success rate
# Model: Claude 3.5 Haiku
# VITE_ANTHROPIC_API_KEY=sk-ant-api03-your_key_here

# ============================================
# OpenAI (Alternative)
# ============================================
# Get your API key from: https://platform.openai.com/
# Best for: Well-tested, reliable performance
# Model: GPT-4o Mini
#
# ⚠️ SECURITY NOTE: VITE_ prefix means this IS exposed to renderer.
# This is a known trade-off for client-side AI features.
# For production, consider implementing server-side proxy or using the
# existing openai-proxy.ts in electron/ipc/ (currently not active).
VITE_OPENAI_API_KEY=your_openai_key_here

# ============================================
# Google Gemini (Alternative)
# ============================================
# Get your API key from: https://aistudio.google.com/
# Best for: Free tier available, fast responses
# Model: Gemini 1.5 Flash
# VITE_GEMINI_API_KEY=your_gemini_key_here

# ============================================
# Ollama (Local, Free but Less Reliable)
# ============================================
# No API key needed - runs locally
# Best for: Privacy, no API costs
# Note: May struggle with strict JSON formatting
# Make sure Ollama is running: ollama serve
VITE_OLLAMA_BASE_URL=http://localhost:11434
VITE_OLLAMA_MODEL=deepseek-r1:1.5b

# ============================================
# Advanced Configuration (Optional)
# ============================================

# Super-Agentic Features (all enabled by default)
VITE_ENABLE_PLANNING=true
VITE_ENABLE_LEARNING=true
VITE_ENABLE_REFLECTION=true

# Max steps per agent run (default: 12 for super-agentic, 8 for basic)
VITE_MAX_AGENT_STEPS=12

# Verbose logging (default: true in super-agentic mode)
VITE_VERBOSE_AGENT_LOGS=true

# ============================================
# cPanel Configuration (SECURE - Main Process Only)
# ============================================
# IMPORTANT: Do NOT use VITE_ prefix for sensitive credentials!
# VITE_ prefixed variables are exposed to the renderer process (browser).
# Only use non-prefixed variables which are securely accessed in main process.
CPANEL_API_TOKEN=your_cpanel_token_here
CPANEL_USERNAME=your_cpanel_username

# ============================================
# GitHub Auto-Update (IMPORTANT)
# ============================================
# GH_TOKEN must be set as a SYSTEM environment variable, NOT in this file!
# This is required for publishing updates to GitHub releases.
#
# On Windows (run in Command Prompt as Admin):
#   setx GH_TOKEN "ghp_your_token_here" /M
#
# On macOS/Linux:
#   export GH_TOKEN="ghp_your_token_here"
#   Or add to ~/.bashrc or ~/.zshrc
#
# To create a token: https://github.com/settings/tokens
# Required scopes: repo (full control)
